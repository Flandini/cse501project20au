num {0, 255} array_average(num {0, 255} [] {0, 50000} numbers)
{
    for ( n : numbers | num acc = 0 )
    {
    	acc = acc + n
    }

    num averageish = acc / array_length (numbers)

    return averageish
}