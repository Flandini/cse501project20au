num {0, 255} array_average(num_iter {0, 255} {0, 50000} numbers)
{
    for ( n : numbers | num acc = 0 )
    {
    	acc + n;
    }

    num averageish = acc / iter_length(numbers);

    return averageish;
}